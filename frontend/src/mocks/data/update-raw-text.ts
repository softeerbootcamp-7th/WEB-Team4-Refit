import type { ApiResponseVoid } from '@/apis/generated/refit-api.schemas'

export const updateRawTextMock: ApiResponseVoid = {
  isSuccess: true,
  code: 'SUCCESS',
  message: 'mock update raw text',
}
